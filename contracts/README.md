# LunaDash SDK 2 contract snapshots

These files are copied from `LuYishan-4/LunaDash@fix/plugin` and are used directly by this repository's validator.

- `targets.json` from `data/plugins/targets.json`
- `SettingsSchema.py` from `cmake/plugins/SettingsSchema.py`
- `ValidatePlugin.py` from `cmake/plugins/ValidatePlugin.py`

When LunaDash changes the SDK contract, update these snapshots in a dedicated infrastructure change before accepting plugins that rely on the new contract.
